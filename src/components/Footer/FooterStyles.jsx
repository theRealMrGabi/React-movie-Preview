import styled from 'styled-components';

const FooterContainer = styled.div`
    background: var(--text);
    color: var(--white);
    margin: 0 !important;
    padding: 0 !important;
`;

export default FooterContainer;